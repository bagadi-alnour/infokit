{
  "$schema": "https://turbo.build/schema.json",
  "globalEnv": [
    "NODE_ENV",
    "DATABASE_URL",
    "DATABASE_URL_MIGRATOR",
    "AUTH_SECRET",
    "AUTH_TRUST_HOST",
    "AUTH_EMAIL_FROM",
    "BOOTSTRAP_SUPERADMIN_EMAIL",
    "AWS_PROFILE",
    "AWS_REGION",
    "AWS_SNS_SENDER_ID",
    "SITE_URL",
    "AUTH_DEV_LOG_DELIVERY",
    "SKIP_ENV_VALIDATION"
  ],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "!.next/cache/**", "dist/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "lint": {},
    "typecheck": {},
    "db:names": {}
  }
}
